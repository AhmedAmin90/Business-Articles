<%= simple_form_for(@category) do |f| %>
  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :priority %>
  </div>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
